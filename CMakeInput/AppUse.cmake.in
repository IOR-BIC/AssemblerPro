INCLUDE(CMakeImportBuildSettings)

CMAKE_IMPORT_BUILD_SETTINGS("${APP_BUILD_SETTINGS_FILE}")

INCLUDE_DIRECTORIES(${APP_INCLUDE_DIRS})

LINK_DIRECTORIES(${APP_LIBRARY_DIRS})

LINK_LIBRARIES(AppCommon AppOperations AppViews AppVME vtkAPP)
